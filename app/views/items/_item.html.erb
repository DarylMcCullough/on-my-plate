<li class="list-group-item"><%= item.name %>
<%= link_to "", item, method: :delete, class: 'glyphicon glyphicon-ok delete_item', remote: true, data: { confirm: 'Are you sure you want to mark this item as done?' } %>
